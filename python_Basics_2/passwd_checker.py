# Password checker by frey ! 
# written on 09062022

username = input('Enter your username :-')
passwd = input('Enter your user passwd :-')

length_passwd = (len(passwd)*'*')

print(f'hello there {username} you are now connected to the wire! and your passwd len is {len(passwd)} letters long {length_passwd}')

